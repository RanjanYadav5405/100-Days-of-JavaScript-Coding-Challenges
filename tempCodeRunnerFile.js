let rev=""
    // for(let i = newStr.length-1 ; i >=0; i--){
    //     rev +=newStr[i]
    // }
    // return rev

    // let doubNum = rev.map((curElem, index) => {
    //      if(index % 2 === 0){
    //         curElem = curElem * 2;
    //      }
    // })
    // console.log(doubNum)